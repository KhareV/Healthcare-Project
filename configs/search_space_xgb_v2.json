{
  "search_space_version": "xgb_search_space_v2",
  "status": "FROZEN_PHASE11_ENTRYPOINT_PHASE12_SAMPLER_NOT_RUN",
  "decision_authority": "USER_DELEGATED_AI_PROJECT_DECISION",
  "family": "xgboost",
  "budget_per_task": 30,
  "parameters": {
    "max_depth": {"minimum": 3, "maximum": 8, "type": "integer"},
    "learning_rate": {"minimum": 0.01, "maximum": 0.20, "type": "continuous", "distribution": "log_uniform"},
    "subsample": {"minimum": 0.60, "maximum": 1.00, "type": "continuous"},
    "colsample_bytree": {"minimum": 0.60, "maximum": 1.00, "type": "continuous"},
    "min_child_weight": {"minimum": 1.0, "maximum": 10.0, "type": "continuous"},
    "reg_alpha": {"minimum": 0.0, "maximum": 1.0, "type": "continuous"},
    "reg_lambda": {"minimum": 0.1, "maximum": 10.0, "type": "continuous"},
    "n_estimators": {"minimum": 1, "maximum": 1000, "type": "integer", "scientific_value": 1000}
  },
  "fixed_execution": {
    "tree_method": "hist",
    "device": "cpu",
    "n_jobs": 1,
    "early_stopping_rounds": 50,
    "restore_or_use_best_iteration": true,
    "serialization": "native_json",
    "numeric_dtype": "float32"
  },
  "objectives": {
    "recovery24": {"objective": "reg:squarederror", "eval_metric": "rmse"},
    "recovery48": {"objective": "reg:squarederror", "eval_metric": "rmse"},
    "icu_stay_time": {"objective": "reg:squarederror", "eval_metric": "rmse"},
    "organ_support": {"objective": "binary:logistic", "eval_metric": "logloss", "probability": "predict_proba_once_uncalibrated"}
  },
  "training_weights": {
    "recovery": "NONE",
    "icu_stay_time": "NONE",
    "organ_support": "PHASE10_SCALE_POS_WEIGHT_ONLY",
    "stay_balanced_weights": "EVALUATION_ONLY_NOT_TRAINING"
  },
  "phase12_sampler": "NOT_EXECUTED_PHASE11",
  "scientific_candidates_executed": 0
}
