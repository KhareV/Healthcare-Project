# Phase 20 Final-Test Evaluation Governance

The real final test is closed. The one canonical Phase-20 command validates the
active G3 marker and every frozen dependency before any loader callback can run.
The current repository has no active G3 marker, so both audit and run commands
fail closed and record zero test-loader calls.

The framework reuses the Phase-9 stay-balanced estimators, Phase-10 ICU-stay
copy bootstrap, Phase-14 complete-component membership, Phase-15 prespecified
slices, the frozen ICU-time transform, and the validation-fitted isotonic
transform. It imports no trainer and exposes no fitting, selection, calibration,
or threshold-search operation.

Real execution also requires a frozen bootstrap replicate count and seed, an
approved pre-test retry policy, a frozen final model-exposure plan, and frozen
error-analysis slice definitions. These remain explicit blockers. The G4 schema
is an unlocked engineering parameter and no G4 marker is created.

Synthetic tests exercise the full guard ordering and one-run behavior. Synthetic
authorization can never authorize the real marker path or real test scope.
